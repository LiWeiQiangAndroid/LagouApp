<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <include
        android:id="@+id/headerView"
        layout="@layout/headerview_layout" />

    <ListView
        android:id="@+id/lv_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/ll_emp"
        android:layout_below="@+id/headerView" >
    </ListView>

    <LinearLayout
        android:id="@+id/ll_emp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="5dp"
        android:orientation="horizontal"
        android:padding="5dp" >

        <TextView
            android:id="@+id/tv_lagou_send"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginLeft="15dp"
            android:background="@drawable/setbar_bg"
            android:layout_weight="1"
            android:gravity="center"
            android:textColor="#00B38A"
            android:text="发送简历" />

        <Button
            android:id="@+id/btn_lagou_talk"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="10dp"
            android:layout_marginRight="10dp"
            android:layout_weight="2"
            android:background="@drawable/select_button_press"
            android:drawableTop="@drawable/shape"
            android:text="和TA聊聊" />
    </LinearLayout>

</RelativeLayout>